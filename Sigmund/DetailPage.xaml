<phone:PhoneApplicationPage
    x:Class="Sigmund.DetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <StackPanel Margin="12,20,12,0">
            <TextBlock x:Name="TxtName" Style="{StaticResource PhoneTextLargeStyle}" TextWrapping="Wrap"/>
            <TextBlock x:Name="TxtPublisher" Style="{StaticResource PhoneTextNormalStyle}" Foreground="{StaticResource PhoneSubtleBrush}"/>

            <Line X1="0" Y1="0" X2="400" Y2="0" Stroke="White" StrokeThickness="2" Margin="12,20,10,20"/>

            <TextBlock Text="VERSION" Style="{StaticResource PhoneTextSubtleStyle}"/>
            <TextBlock x:Name="TxtVersion" Style="{StaticResource PhoneTextNormalStyle}" Margin="0,0,0,15"/>

            <TextBlock Text="INSTALLED ON" Style="{StaticResource PhoneTextSubtleStyle}"/>
            <TextBlock x:Name="TxtDate" Style="{StaticResource PhoneTextNormalStyle}" Margin="0,0,0,15"/>

            <TextBlock Text="INSTALL PATH" Style="{StaticResource PhoneTextSubtleStyle}"/>
            <TextBlock x:Name="TxtPath" Style="{StaticResource PhoneTextNormalStyle}" TextWrapping="Wrap" Margin="0,0,0,30"/>

            <Button x:Name="BtnLaunch" Content="LAUNCH APP" Click="BtnLaunch_Click" Margin="0,20,0,0"/>
            <Button x:Name="BtnUninstall" Content="UNINSTALL" Click="BtnUninstall_Click" Foreground="Red" BorderBrush="Red"/>
        </StackPanel>
    </Grid>

</phone:PhoneApplicationPage>